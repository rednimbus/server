extends layout

block content
    h1= title

    ul
    each author in author_list
        li 
            a(href=author.url) #{author.name}
            if !author.date_of_birth && !author.date_of_death
                | （生卒不详）
            else
                | （#{author.date_of_birth_format || '不详'}至#{author.date_of_death_format || '今'}）
    else
        li 暂无作者信息